sensors:
    phoxi:
        type: colorized_phoxi
        frame: phoxi
        phoxi_config:
            # device_name: 2018-02-020-LC3
            device_name: 1703005
            size: small
        webcam_config:
            device_id: 0
        calib_dir: /nfs/diskstation/calib/

# Insertion
# workspace:
#     min_pt:
#      - 0.25
#      - -0.15
#      - 0.002
#     max_pt:
#      - 0.55
#      - 0.15
#      - 0.4

# Orienting Objects CASE Paper
# workspace:
#     min_pt:
#      - 0.3
#      - -0.2
#      - 0.18
#     max_pt:
#      - 0.4
#      - 0
#      - 0.4

# Green Card Plane
plane_workspace:
    min_pt:
     - 0.165 #bottom of image, part next to yumi base
     - -0.375 # right of image, part next to Hermes
     - -0.0
    max_pt:
     - 0.645 #top of image
     - 0.335 # left of image
     - 0.6

box_workspace:
    min_pt:
     - 0.165 #bottom of image, part next to yumi base
     - -0.375 # right of image, part next to Hermes
     - 0.359
    max_pt:
     - 0.645 #top of image
     - 0.335 # left of image
     - 0.6

negative_workspace:
    min_pt:
     - 0.3 #bottom of image, part next to yumi base
     - 0.10 # right of image, part next to Hermes
     - -0.0
    max_pt:
     - 0.525 #top of image
     - 0.25 # left of image
     - 0.105

# Full above table
    # min_pt:
    #  - 0.21
    #  - -0.425
    #  - 0.18
    # max_pt:
    #  - 0.645
    #  - 0.425
    #  - 0.4
